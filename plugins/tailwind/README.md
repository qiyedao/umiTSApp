 # 配置
 ```
 tailwindcss: {
    // tailwindCssFilePath: '@/tailwind.css',//指定后需手动创建文件,默认放入src/.umi/tailwind.css
    tailwindConfigFilePath: 'tailwind.config.js', // 默认取值 tailwindConfigFilePath || join(process.env.APP_ROOT || api.cwd, 'tailwind.config.js'),,
  },
 ```